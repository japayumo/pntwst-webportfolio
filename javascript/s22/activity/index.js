// console.log('Hello');

/*function userWelcome(){
	let userType = prompt("Enter user type: ").toLowerCase();
	console.log('Role: '+ userType);
	switch (userType){
		case 'admin':
			return (`Welcome back to the class portal, ${userType}!`);
			break;

		case 'teacher':
			return (`Thank you for logging in, ${userType}!`);
			break;

		case 'student':
			return (`Welcome to the class portal, ${userType}!`);
			break;

		default:
			return (`Role out of range.`);
			break;
	}
		
		
};
*/
